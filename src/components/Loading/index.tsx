import style from '@/styles/Loading.module.css'

const Loading = () => {
    return (
        <main className={style.loading__page}>
                <span className={style.loading} />
        </main>
    )
}
export default Loading