import Form from '@/components/Form'
import style from '@/styles/Login.module.css'

const Login = () => {
    return (
        <main className={style.login__page}>
            <Form />
        </main>
    )
}

export default Login